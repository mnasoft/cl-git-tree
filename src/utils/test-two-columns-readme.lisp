;;; Демонстрация двухколоночного вывода с содержимым README.org

(require 'asdf)
(asdf:load-system :cl-git-tree/utils)

(use-package :cl-git-tree/utils/two-columns)

;; Выведем список всех команд из README.org
(defparameter *all-commands*
  '("add"                  ; Добавить файлы в индекс
    "aliases"              ; Показать команды
    "all"                  ; pull → add → commit → push
    "audit"                ; Проверка состояния репозиториев
    "clone"                ; Клонировать все локальные репозитории
    "commit"               ; Сделать git commit во всех репозиториях
    "help"                 ; Показать справку
    "info"                 ; Показать информацию о системе
    "locations"            ; Управление локациями
    "patterns"             ; Управление паттернами файлов
    "pull"                 ; Выполнить git pull во всех репозиториях
    "push"                 ; Выполнить git push во всех репозиториях
    "remake-xz"            ; Пересоздание архивов
    "remote"               ; Управление remote
    "transport"            ; Архивация репозиториев в tar.xz
    "version"))            ; Показать версию

;; Основные компоненты системы
(defparameter *system-modules*
  '("cli.lisp"             ; CLI точка входа
    "config.lisp"          ; Загрузка конфигурации при старте
    "commands/"            ; 23 CLI-команды
    "dispatch/"            ; Реестр и диспетчер команд
    "loc/"                 ; Управление локациями
    "fs/"                  ; Утилиты файловой системы
    "git-utils/"           ; Обёртки для git-команд
    "shell-utils/"         ; Выполнение shell-команд
    "config/"              ; Управление конфигурациями
    "global/"              ; Глобальные переменные git
    "utils/"               ; Утилиты форматирования
    "tests/"               ; Набор тестов
    "package.lisp"         ; Основной пакет
    "cli-script.lisp"))    ; Скрипт для запуска

;; Возможности системы
(defparameter *features*
  '("Регистрация локаций"           ; в глобальной таблице *locations*
    "Поддержка провайдеров"         ; :local, :github, :gitlab
    "Синхронизация репозиториев"    ; clone/unclone
    "Git-операции"                  ; add, commit, pull, push
    "Управление remotes"            ; add/remove/readd/create/delete
    "Офлайн-транспорт"              ; архивы tar.xz
    "Конфигурация и вспомощь"       ; управление шаблонами
    "Универсальное применение"      ; все операции ко всем репозиториям
    "API на Common Lisp"            ; полный доступ через Lisp
    "CLI из командной строки"       ; через SBCL
    "Полное покрытие тестами"       ; 122+ тестов
    "Поддержка MSYS2"               ; на Windows
    "Автоматическая загрузка"       ; конфигурация из файла
    "Валидация URL"                 ; нормализация путей
    "Парсинг SSH"                   ; работа с SSH URL
    "Поиск репозиториев"            ; автоматический скан"))

(format t "~%╔════════════════════════════════════════════════════════════════════════════╗~%")
(format t "║         СОДЕРЖИМОЕ README.org В ДВЕ КОЛОНКИ - ДЕМОНСТРАЦИЯ ФУНКЦИИ        ║~%")
(format t "╚════════════════════════════════════════════════════════════════════════════╝~%~%")

;; Команды
(format t "~%📋 ДОСТУПНЫЕ КОМАНДЫ (16 команд)~%~%")
(print-two-columns *all-commands* :column-width 22 :separator "  ")

;; Модули
(format t "~%📁 КОМПОНЕНТЫ СИСТЕМЫ (14 модулей)~%~%")
(print-two-columns *system-modules* :column-width 22 :separator "  ")

;; Возможности
(format t "~%✨ ОСНОВНЫЕ ВОЗМОЖНОСТИ (16 функций)~%~%")
(print-two-columns *features* :column-width 38 :separator "  ")

(format t "~%✅ Функция print-two-columns работает правильно!~%~%")
